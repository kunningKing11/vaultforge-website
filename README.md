# VaultForge Website

A basic React and TailwindCSS demo landing page for the [VaultForge Wallet](https://github.com/kunningKing11/vaultforge) project.

## Development

Install dependencies with Bun:

```bash
bun install
```

Run locally:

```bash
bun run dev
```

Build for production:

```bash
bun run build
```
